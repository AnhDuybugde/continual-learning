# Reproduction environment captured by the ETTh1 engineering smoke run.
# Torch's CUDA build is platform/index specific; keep the exact local build
# recorded here and install from the matching PyTorch wheel index.
numpy==2.2.6
pandas==2.3.3
torch==2.6.0+cu124
psutil==7.2.2
matplotlib==3.10.8
wandb==0.28.2
wandb==0.28.2
